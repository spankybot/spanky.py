name: SpankyTests

on: [push, pull_request]

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - name: unit tests
        run: |
          cd ${GITHUB_WORKSPACE}
          pwd
          ./Dockerfile/build.sh && ./Dockerfile/run_tests.sh
        
